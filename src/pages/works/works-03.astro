---
import HeaderWork from '../../components/HeaderWork.astro';
import FooterWork from '../../components/FooterWork.astro';
import Abtest from '../../components/Abtest.astro';
import Breadcrumb from '../../components/Breadcrumb.astro';
---

<!DOCTYPE html>
<html lang="ja">
<head>
 <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>Ryota Tomisaki Portfolio Site</title>
     <meta name="description" content="Ryota Tomisakiのポートフォリオサイト。デザイン・コーディングの実績を紹介しています。">
     <meta name="keywords" content="Ryota Tomisaki, ポートフォリオ, デザイン, コーディング, Web制作,デザイナー,冨崎涼太">
    <link rel="canonical" href="https://portfolio-site-snowy-omega.vercel.app/">
    
    <!-- Open Graph（SNSシェア用） -->
    <meta property="og:title" content="Ryota Tomisaki Portfolio Site">
    <meta property="og:description" content="デザインとコーディングの実績を紹介するポートフォリオサイトです。">
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://portfolio-site-snowy-omega.vercel.app">

    <!-- Twitterカード -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="Ryota Tomisaki Portfolio Site">
    <meta name="twitter:description" content="デザインとコーディングの実績を紹介するポートフォリオサイトです。">
    <link rel="icon" type="image/png" href="/images/icon.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/images/app-icon.png">
    <title>Ryota Tomisaki Portfolio Site</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://use.typekit.net/xab0ngk.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-X00CFV5WYJ"></script>
    <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());

    gtag('config', 'G-X00CFV5WYJ');
    </script>
    <script type="text/javascript">
    (function(c,l,a,r,i,t,y){
        c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
        t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
        y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
    })(window, document, "clarity", "script", "ti6yncp6bw");
    </script>
   
</head>

    <HeaderWork />
    <main class="main work-main">
            <div class="bc_top">
        <Breadcrumb />
        </div>
        <div class="works_visual fade-in">
            <img src="../images/work_pc-03.jpg" alt="">
        </div>
        <div class="bc_bottom">
        <Breadcrumb />
        </div>
    </main>

    <div class="works-arrow">
        <div>
            <a href="works-02">&lt; prev</a>
            <a href="works-04">next &gt;</a>
        </div>
    </div>

    <div class="work_page-title">
        <h2><small>美容モニター募集サイト</small>
            <br>ヴィーナスウォーカー</h2>
        <span class="work_page-subtitle">WEB Design</span>
        <ul class="work-url">
                <li><span>[制作期間]</span>2023.12.15~2025.8.25</li>
                <li><span>[テーマ]</span>ヴィーナスウォーカー</li>
                <li><span>[ターゲット]</span>20~30代 女性</li>
                <li><span>[PJ人数]</span>2人(デザイン/コーディングを担当)</li>
                <li><span>[使用ツール]</span>Illustrator / Photoshop / figma / VScode(html/css/js)</li>
                <li><span>[サイトURL]</span><a href="hhttps://venus-walker.com/lp/">https://venus-walker.com/lp/</a></li>
        </ul>
        <div class="line"></div>
        <h3 class="work_about">サイト概要</h3>
        <p>
            株式会社ノーマークにてデザインを担当。ヴィーナスウォーカーのLPやログイン後ページの改修を行い、
            新規ユーザーが登録しやすい雰囲気作りのためデザインを刷新。<strong>主にサイトバナーやLP・マイページのデ
            ザイン改修、HTML/CSS/JSによるコーディング、必要に応じてPHPの簡単な追記も対応。</strong>
        </p>
            <section class="section site_detail">
            <div class="work_detail">
            <div class="detail">
            <h4>やったこと</h4>
            <h3>「既存デザインの改修」</h3>
            <p class="text">
            デザイン改修を担当し、ログイン後バナーの制作やURLごとの条件分岐によるユーザー属性に合わせたデザインを実
            装、GitHubでソースを管理しつつ、<strong>HTML/CSS/JavaScriptによる修正とUI改善も一貫して対応</strong>しました。
            </p>
            </div>
            <div class="detail">
            <h4>施策</h4>
            <h3>「Clarity導入」</h3>
            <p class="text">
            <strong>既存のHotjarではなくClarityの導入を提案</strong>し、VenusWalkerに加えPocketResearchやスマモニにも適用しました。
            これにより、より<strong>詳細な数値検証が可能</strong>になりました。
            </p>
            <img src="/images/work_about03-3.png" alt="clarity">
        </div>
    </div>

    <div class="content">
    <div class="line-about"></div>
    </div>
    <picture>
    <source media="(max-width: 820px)" srcset="/images/work_about03-2.png">
    <img src="/images/work_about03.png" alt="画像" class="image">
    </picture>
    </section>

    <div class="work-img site-content">
    <div class="site_only">
        <img src="../images/pc-img03.webp" alt="" class="fade-in">
        <img src="../images/sp-img03.webp" alt="" class="fade-in">
    </div>
    </div>
</div>
    </main>
    <FooterWork />
    <script src="https://code.jquery.com/jquery-3.4.1.min.js" integrity="sha256-CSXorXvZcTkaix6Yvo6HppcZGetbYMGWSFlBw8HfCJo=" crossorigin="anonymous"></script>
    <script src="../js/text.js" type="module"></script>
    <script src="../js/scroll.js" type="module"></script>
    <script>
        // フェードイン
function showElementAnimation() {
    var element = document.getElementsByClassName('fade-in');
    if (!element) return;
    var showTiming = window.innerHeight > 768 ? 200 : 60;
    var scrollY = window.pageYOffset;
    var windowH = window.innerHeight;
    for (var i = 0; i < element.length; i++) {
        var elemClientRect = element[i].getBoundingClientRect(); var elemY = scrollY + elemClientRect.top; if (scrollY + windowH - showTiming > elemY) {
            element[i].classList.add('show');
        } else if (scrollY + windowH < elemY) {
        }
    }
}
showElementAnimation();
window.addEventListener('scroll', showElementAnimation);
</script>
<script>
    // IE、Safari対応
// smoothscroll.js読み込み
// https://github.com/iamdustan/smoothscroll

// セレクタ名（.pagetop）に一致する要素を取得
const pagetop_btn = document.querySelector(".pagetop");

// .pagetopをクリックしたら
pagetop_btn.addEventListener("click", scroll_top);

// ページ上部へスムーズに移動
function scroll_top() {
  window.scroll({ top: 0, behavior: "smooth" });
}
</script>
    <script>
        const drawerBtn = document.querySelector('.drawer-btn');
        const drawerNav = document.querySelector('.drawer-nav');
        const drawerLinks = drawerNav.querySelectorAll('a'); // メニュー内のリンク

        // ボタンで開閉
        drawerBtn.addEventListener('click', () => {
            if (drawerNav.classList.contains('active')) {
                drawerNav.classList.remove('active');
                drawerNav.classList.add('closing');
                drawerBtn.classList.remove('active-btn');
            } else {
                drawerNav.classList.add('active');
                drawerNav.classList.remove('closing');
                drawerBtn.classList.add('active-btn');
            }
        });

        // メニュー内リンククリックで閉じる
        drawerLinks.forEach(link => {
            link.addEventListener('click', () => {
                drawerNav.classList.remove('active');
                drawerNav.classList.add('closing');
                drawerBtn.classList.remove('active-btn');
            });
        });
    </script>

      <script>
const sections = document.querySelectorAll('.section');

function handleScroll() {
  const triggerBottom = window.innerHeight * 0.8;

  sections.forEach(section => {
    const sectionTop = section.getBoundingClientRect().top;
    const img = section.querySelector('.image');
    const line = section.querySelector('.line-about');
    const details = section.querySelectorAll('.work_detail .detail');

    if (sectionTop < triggerBottom && !section.classList.contains('animated')) {
      section.classList.add('animated');

      // 画面幅チェック
      const isSmall = window.innerWidth <= 650;

      // 1. 画像フェードイン
      if (img) img.style.opacity = 1;

      if (!isSmall && line) {
        // 2. 線を伸ばす（650px以上のみ）
        setTimeout(() => {
          line.style.transform = 'scaleX(1)';
        }, 500);
      }

      // 3. 文章は常に順番にフェードイン
      setTimeout(() => {
        details.forEach((detail, index) => {
          setTimeout(() => {
            detail.style.opacity = 1;
            detail.style.transform = 'translateX(0)';
          }, index * 300);
        });
      }, isSmall ? 500 : 1600); // 線が無い場合は少し早めに
    }
  });
}

window.addEventListener('scroll', handleScroll);
window.addEventListener('resize', handleScroll);
handleScroll();

    </script>
</body>
</html>